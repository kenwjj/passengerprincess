# Telegram bot token from BotFather (/newbot). Do NOT commit the real .env.
BOT_TOKEN=

# Anthropic API key (console.anthropic.com) — used for itinerary generation.
ANTHROPIC_API_KEY=
# Optional override:
# ITINERARY_MODEL=claude-sonnet-4-6
